# v0.4.0 안정성 구현 계획

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** NestJS v10/v11 × swagger v7/v8/v11 CI 매트릭스를 추가하고, 비정형 데이터 엣지 케이스 테스트를 보강하여 v1.0.0 안정화의 근거를 마련한다.

**Architecture:** CI 워크플로우에 버전 매트릭스를 추가하여 기존 테스트를 다양한 버전 조합에서 실행한다. 소스 코드 변경 없이 테스트와 CI만 확장한다. 엣지 케이스는 유닛 + E2E 테스트로 검증한다.

**Tech Stack:** GitHub Actions, NestJS 10/11, @nestjs/swagger 7/8/11, Jest 30, supertest

**Spec:** `docs/superpowers/specs/2026-03-22-v0.4.0-stability-design.md`

---

## 파일 구조

| 파일 | 역할 | 변경 유형 |
|------|------|-----------|
| `.github/workflows/ci.yml` | CI 매트릭스 — NestJS/swagger 버전 축 추가 | 수정 |
| `src/interceptors/safe-response.interceptor.spec.ts` | 유닛 테스트 — 비정형 데이터 6개 추가 | 수정 |
| `test/e2e/test.controller.ts` | E2E 컨트롤러 — 엣지 케이스 라우트 4개 추가 | 수정 |
| `test/e2e/safe-response.e2e-spec.ts` | E2E 테스트 — 엣지 케이스 4개 추가 | 수정 |
| `README.md` | Buffer/Stream 안내 추가 | 수정 |
| `README.ko.md` | 동일 내용 한국어 | 수정 |
| `CHANGELOG.md` | v0.4.0 항목 | 수정 |
| `docs/roadmap.md` | v0.4.0 완료 표시 | 수정 |
| `package.json` | 버전 0.3.0 → 0.4.0 | 수정 |

---

## Task 1: CI 매트릭스 확장

**Files:**
- Modify: `.github/workflows/ci.yml`

- [ ] **Step 1: ci.yml을 NestJS/swagger 버전 매트릭스로 교체**

`.github/workflows/ci.yml` 전체를 다음으로 교체:

```yaml
name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  test:
    runs-on: ubuntu-latest

    strategy:
      fail-fast: false
      matrix:
        node-version: [18, 20, 22]
        nestjs-version: [10, 11]
        swagger-version: [7, 8, 11]
        exclude:
          - nestjs-version: 10
            swagger-version: 11

    name: Node ${{ matrix.node-version }} / NestJS ${{ matrix.nestjs-version }} / Swagger ${{ matrix.swagger-version }}

    steps:
      - uses: actions/checkout@v4

      - name: Use Node.js ${{ matrix.node-version }}
        uses: actions/setup-node@v4
        with:
          node-version: ${{ matrix.node-version }}
          cache: npm

      - run: npm ci

      - name: Override NestJS version
        if: matrix.nestjs-version != 11
        run: |
          npm install --no-save \
            @nestjs/common@^${{ matrix.nestjs-version }} \
            @nestjs/core@^${{ matrix.nestjs-version }} \
            @nestjs/platform-express@^${{ matrix.nestjs-version }} \
            @nestjs/testing@^${{ matrix.nestjs-version }}

      - name: Override Swagger version
        if: matrix.swagger-version != 11
        run: |
          npm install --no-save @nestjs/swagger@^${{ matrix.swagger-version }}

      - run: npm run build
      - run: npm test
      - run: npm run test:e2e
```

- [ ] **Step 2: 커밋**

```bash
git add .github/workflows/ci.yml
git commit -m "ci: add NestJS v10/v11 × swagger v7/v8/v11 version matrix

Co-Authored-By: Claude Opus 4.6 (1M context) <noreply@anthropic.com>"
```

---

## Task 2: 엣지 케이스 유닛 테스트

**Files:**
- Modify: `src/interceptors/safe-response.interceptor.spec.ts` (파일 끝의 `});` 직전에 describe 블록 추가)

- [ ] **Step 1: 비정형 데이터 유닛 테스트 6개 작성**

`src/interceptors/safe-response.interceptor.spec.ts`의 마지막 `describe` 블록 뒤 (파일 끝의 `});` 직전)에 추가:

```typescript
  // ─── 엣지 케이스: 비정형 데이터 ───

  describe('엣지 케이스: 비정형 데이터', () => {
    beforeEach(() => {
      jest.spyOn(reflector, 'get').mockReturnValue(undefined);
    });

    it('undefined 반환 → data가 undefined', async () => {
      const interceptor = createInterceptor();
      const ctx = createMockExecutionContext();

      const result = await lastValueFrom(
        interceptor.intercept(ctx, createMockCallHandler(undefined)),
      );

      expect(result.success).toBe(true);
      expect(result.data).toBeUndefined();
    });

    it('빈 객체 반환 → data가 빈 객체', async () => {
      const interceptor = createInterceptor();
      const ctx = createMockExecutionContext();

      const result = await lastValueFrom(
        interceptor.intercept(ctx, createMockCallHandler({})),
      );

      expect(result.data).toEqual({});
    });

    it('빈 배열 반환 → data가 빈 배열', async () => {
      const interceptor = createInterceptor();
      const ctx = createMockExecutionContext();

      const result = await lastValueFrom(
        interceptor.intercept(ctx, createMockCallHandler([])),
      );

      expect(result.data).toEqual([]);
      expect(Array.isArray(result.data)).toBe(true);
    });

    it('숫자 반환 → data가 숫자', async () => {
      const interceptor = createInterceptor();
      const ctx = createMockExecutionContext();

      const result = await lastValueFrom(
        interceptor.intercept(ctx, createMockCallHandler(42)),
      );

      expect(result.data).toBe(42);
    });

    it('boolean 반환 → data가 boolean', async () => {
      const interceptor = createInterceptor();
      const ctx = createMockExecutionContext();

      const result = await lastValueFrom(
        interceptor.intercept(ctx, createMockCallHandler(true)),
      );

      expect(result.data).toBe(true);
    });

    it('중첩 객체 반환 → data에 중첩 구조 유지', async () => {
      const interceptor = createInterceptor();
      const ctx = createMockExecutionContext();
      const nested = { a: { b: { c: 1 } } };

      const result = await lastValueFrom(
        interceptor.intercept(ctx, createMockCallHandler(nested)),
      );

      expect(result.data).toEqual({ a: { b: { c: 1 } } });
    });
  });
```

- [ ] **Step 2: 유닛 테스트 실행**

Run: `npx jest src/interceptors/safe-response.interceptor.spec.ts --verbose 2>&1 | tail -15`
Expected: 기존 36개 + 신규 6개 = 42개 전체 통과

- [ ] **Step 3: 커밋**

```bash
git add src/interceptors/safe-response.interceptor.spec.ts
git commit -m "test: add edge case unit tests for non-standard data types

Co-Authored-By: Claude Opus 4.6 (1M context) <noreply@anthropic.com>"
```

---

## Task 3: 엣지 케이스 E2E 테스트 인프라 + 테스트

**Files:**
- Modify: `test/e2e/test.controller.ts` (라우트 4개 추가)
- Modify: `test/e2e/safe-response.e2e-spec.ts` (테스트 스위트 추가)

- [ ] **Step 1: 테스트 컨트롤러에 엣지 케이스 라우트 추가**

`test/e2e/test.controller.ts`의 TestController 클래스 맨 아래에 4개 라우트 추가:

```typescript
  @Get('edge-undefined')
  edgeUndefined() {
    return undefined;
  }

  @Get('edge-null')
  edgeNull() {
    return null;
  }

  @Get('edge-buffer')
  edgeBuffer() {
    return Buffer.from('hello');
  }

  @Get('edge-empty-string')
  edgeEmptyString() {
    return '';
  }
```

- [ ] **Step 2: E2E 테스트 추가**

`test/e2e/safe-response.e2e-spec.ts`의 마지막 `});` 직전에 추가:

```typescript
  // ─── 엣지 케이스: 비정형 데이터 ───

  describe('엣지 케이스: 비정형 데이터', () => {
    beforeEach(async () => {
      app = await createApp(TestAppModule);
    });

    it('GET /test/edge-undefined → data 프로퍼티가 JSON에서 생략됨', async () => {
      const res = await request(app.getHttpServer())
        .get('/test/edge-undefined')
        .expect(200);

      expect(res.body.success).toBe(true);
      expect(res.body).not.toHaveProperty('data');
    });

    it('GET /test/edge-null → data가 null', async () => {
      const res = await request(app.getHttpServer())
        .get('/test/edge-null')
        .expect(200);

      expect(res.body.success).toBe(true);
      expect(res.body.data).toBeNull();
    });

    it('GET /test/edge-buffer → Buffer가 JSON 직렬화됨 (문서 경고 근거)', async () => {
      const res = await request(app.getHttpServer())
        .get('/test/edge-buffer')
        .expect(200);

      expect(res.body.success).toBe(true);
      expect(res.body.data).toHaveProperty('type', 'Buffer');
      expect(res.body.data).toHaveProperty('data');
      expect(Array.isArray(res.body.data.data)).toBe(true);
    });

    it('GET /test/edge-empty-string → data가 빈 문자열', async () => {
      const res = await request(app.getHttpServer())
        .get('/test/edge-empty-string')
        .expect(200);

      expect(res.body.success).toBe(true);
      expect(res.body.data).toBe('');
    });
  });
```

- [ ] **Step 3: E2E 테스트 실행**

Run: `npx jest --config test/jest-e2e.json --verbose 2>&1 | tail -20`
Expected: 기존 27개 + 신규 4개 = 31개 전체 통과 (1 skipped 포함)

- [ ] **Step 4: 전체 테스트 확인**

Run: `npx jest --verbose 2>&1 | tail -5`
Expected: 유닛 + E2E 전체 통과

- [ ] **Step 5: 커밋**

```bash
git add test/e2e/test.controller.ts test/e2e/safe-response.e2e-spec.ts
git commit -m "test: add edge case E2E tests for undefined, null, Buffer, empty string

Co-Authored-By: Claude Opus 4.6 (1M context) <noreply@anthropic.com>"
```

---

## Task 4: 문서 업데이트 및 릴리스 준비

**Files:**
- Modify: `README.md`, `README.ko.md`, `CHANGELOG.md`, `docs/roadmap.md`, `package.json`

- [ ] **Step 1: package.json 버전 업데이트**

`package.json`의 `"version"` 필드를 `"0.4.0"`으로 변경.

- [ ] **Step 2: CHANGELOG.md 업데이트**

기존 내용 위에 v0.4.0 항목 추가:

```markdown
## [0.4.0] - 2026-03-22

### Added
- CI matrix: NestJS v10/v11 × @nestjs/swagger v7/v8/v11 (15 combinations × 3 Node versions)
- Edge case unit tests: undefined, empty object, empty array, number, boolean, nested object
- Edge case E2E tests: undefined, null, Buffer, empty string
- Buffer/Stream documentation: `@RawResponse()` usage guide

### Changed
- CI workflow: `fail-fast: false` for complete matrix visibility

[0.4.0]: https://github.com/ksyq12/nestjs-safe-response/releases/tag/v0.4.0
```

- [ ] **Step 3: README.md에 Buffer/Stream 안내 추가**

README.md를 읽고, 적절한 위치(Decorators 섹션의 `@RawResponse()` 설명 근처)에 추가:

```markdown
> **Note:** If your controller returns a `Buffer` or `Stream`, use `@RawResponse()` to skip response wrapping. Without it, binary data will be serialized as `{ type: 'Buffer', data: [...] }`, which corrupts the original content.
```

- [ ] **Step 4: README.ko.md 동일 업데이트**

한국어 README에도 동일한 Buffer/Stream 안내 추가:

```markdown
> **참고:** 컨트롤러가 `Buffer`나 `Stream`을 반환하는 경우 `@RawResponse()`를 사용하세요. 그렇지 않으면 바이너리 데이터가 `{ type: 'Buffer', data: [...] }` 형태로 직렬화되어 원본 내용이 손상됩니다.
```

- [ ] **Step 5: docs/roadmap.md 업데이트**

v0.4.0 항목을 완료 표시로 변경.

- [ ] **Step 6: 전체 빌드 및 테스트**

Run: `npm run build && npm test && npm run test:e2e`
Expected: 빌드 성공, 전체 테스트 통과

- [ ] **Step 7: 커밋**

```bash
git add package.json CHANGELOG.md README.md README.ko.md docs/roadmap.md
git commit -m "chore: bump version to 0.4.0 and update docs

Co-Authored-By: Claude Opus 4.6 (1M context) <noreply@anthropic.com>"
```

---

## Task 5: CI 매트릭스 검증 (push 후)

이 태스크는 push 후에 수행한다.

- [ ] **Step 1: push**

```bash
git push origin main
```

- [ ] **Step 2: CI 매트릭스 실행 확인**

Run: `gh run list --limit 1 --json databaseId,status`
그 후: `gh run watch <id> --exit-status`

Expected: 15개 매트릭스 조합 전체 통과. 실패하는 조합이 있으면 원인 분석 후 대응 (코드 수정 또는 peerDeps 범위 조정).

- [ ] **Step 3: 실패 조합 대응 (필요 시)**

실패한 조합이 있으면:
- 에러 로그 확인: `gh run view <id> --log-failed`
- API 호환 문제면 소스 코드 수정
- 근본적 비호환이면 peerDeps에서 해당 버전 제거
- 수정 후 다시 push하여 CI 재실행
